# تقرير الدمج الشامل — PS4 FW 13.52 (HeerBSD r228995)
## التحديث النهائي: 2026-09-15 — إغلاق ما تبقّى من طبقة syscall

> هذا الملف هو المرجع الدائم لجميع العمليات والبدائيات والنتائج على هذا الجهاز،
> مع مقارنتها بالمعرفة المنشورة (المشهد العام) ونقاط التصحيح الذاتي.
> المصدر: جلسات حية على FW 13.52 (PS4 IP 192.168.100.61، uid=1 من اللعبة/المتصفح).

---

## 1. هوية النظام (مثبتة، غير مفترضة)
| البند | القيمة | مصدر الإثبات |
|-------|--------|--------------|
| FW | 13.52 (0x0D 0x34 kern.sdk_version) | sysctl حي |
| النواة | HeerBSD r228995، ABI FreeBSD **13.0** (وليس 9) | libkernel stubs + psdevwiki |
| الحجم | 589824 بايت (libkernel.bin، جلسة 13.52) | dump محلي |
| الأرقام المخصصة | سوني 585–677؛ القياسية = FreeBSD 13.0 | جدول الـ stubs المدمج |
| آخر سوني مخصص معروف | #677 (في psdevwiki) | فحص إنترنتي |

---

## 2. البدائيات العاملة (CONFIRMED LIVE، عبر المتصفح/اللعبة)
| البدائية | الوظيفة | القيد المُلزم |
|----------|---------|---------------|
| memory.read/write_byte/word/dword/qword | R/W مستخدم-فضاء | الصفحات المخططة فقط |
| memory.read/write_buffer | قراءة/كتابة الحزم | نفس القيد |
| memory.alloc | calloc صفري | صفحة معينة |
| native.fcall | استدعاء stub syscall (بعنوان libkernel) | uid=1، تُمرر عبر الـ stubs فقط |
| socketpair(AF_UNIX) | IPC ثنائي الاتجاه | bind/listen/SCM_RIGHTS محجوبة |
| socket(AF_INET,STREAM) | fd حقيقي | bind/connect/ip_ctloutput محجوبة |
| kqueueex #362/#363 | تَسجّل الأحداث | **لا تُسلّم فعليًا**؛ ext[4]=0، لا تسريب |
| gettimeofday, getrusage, sysctl kern.* | معلومات نواة آمنة | لا مؤشرات kernel |
| syscall 585 is_in_sandbox | يعيد 1 | — |

---

## 3. ما أُغلِق نهائيًا (كل الحملات السابقة) — التحقق النهائي
### 3.1 متجهات kernel من uid=1 (لعبة/متصفح) — كلها ميتة
| السطح | النتيجة | الدليل |
|-------|---------|--------|
| fork + ptrace + chroot | مهجور | كلها TRIGGER كراش |
| kevent STD #363 (COMPAT11) | بيانات منقاة فقط | ext[4]=0 |
| ربط ioctl على /dev بجميع الأجهزة | جلوجة | ioctl: كلها -1 أو EINVAL/ENOTTY |
| dmem0 internal fd (libkernel 5/6/7) | نافذة 16MB dmap فقط | لا تسريب، لا سطح kernel R/W |
| ioctl 0x80108002 + mmap (dmem0) | 😊 يَعمل لكن bلة GPU مجمّدة | لا DMA redirect، ثابت (r9am) |
| netevent (أوامر 585–677) | **مستنفذ نهائيًا** | تفريغ شامل 3 أوامر حية فقط |
| custom syscalls 585–677 | كلها -1/ENOSYS أو -1 | جدول حي |
| syscall 434/435 | **لم يُسبرا بعد** — الوحيدان الباقيان | يتطلب تمهيدًا نظيفًا |
| كل /dev (gbase/dce/... ) | ENOENT أو محجوبة | فحص مسح |
| net.* sysctl | كلها مغلقة | sysctl حي |
| W^X / ASLR | مفعّلان | native.fcall يستدعي syscall stubs فقط |

### 3.2 تفصيل خاص: netevent — أُغلق نهائيًا
- **#99 netcontrol** يعمل عبر netevent فقط (3 أوامر مستخدمة: 0x20000003/0x20000007/0x20000010)، لكنه لا يُخرج أي أمر تسجيل خفي للـ queue، ولا يوجد أمر يفحص المؤشرات.
- **الـ ucred UAF عبر netevent = غير قابل للوصول من uid=1** (سلسلة مولّدة للوصول uid=0 فقط عبر BD-J).
- ⚠️ التصحيح الذاتي الأهم: «الختم الثنائي» (setuid(1)×2) الذي نظن أنه يحرر ucred **مرفوض من 13.52** (لا setreuid في uid=1) — لا يوجد أي مستقبل.

### 3.3 تفصيل خاص: dmem0 (نافذة GPU المجمّدة) — أُغلق نهائيًا
- **الوصول**: يُفتح عبر fd داخلي من libkernel (dmem0@fd5)، ويحصل على:
  - دالة ioctl 0x80108002 (إعادة توطين نافذة 16MB)
  - دالة mmap → R/W مدمج 16MB في kernel dmap
  - لا تغيير في المحتوى عبر الوقت (نافذة مجمّدة = وصف GPU سوني للكتابة المستقرة)
- **لا يمكن**: توسيع النافذة، DMA redirect، كتابة kernel R/W، استعلام المؤشرات، حذف الواصف — كل ioctl مربوط بإعادة تهيئة GPU/حالة اللعبة، وليس بربط kernel.
- **الاستنتاج**: ليست أولية kernel R/W، بل قفزة ABI. البند مغلق.

### 3.4 syscall سوني المخصصة 585–677 — كلها مختبرة
- السجلات المستهدفة (التفريغ الإجمالي، كل رقم 0x20–0x2A وX0x20... ) → كلها ENOSYS أو مهجورة، عددا 585 (is_in_sandbox=1).
- **#434/#435 (آخرا الأرقام المعروفة غير المعروفين) — الوحيدان غير المسبورين.**

### 3.5 التصحيح الذاتي الحاسم (ما كنا مخطئين فيه سابقًا)
| الادّعاء السابق | التصحيح المنشور/الحي |
|-----------------|-----------------------|
| «تسريب /dev هو المفتاح» | كل /dev مغلق من اللعبة والمتصفح؛ لا تسريب |
| «sc#434/435 اكتشفنا» | **لا** — لم يُسبرا بعد (هذا التقرير) |
| «النواة 13.52 محصّنة كليًا» | **غير صحيح تمامًا**: Poopsploit (BD-J) يصل إليها، لكن سطح من اللعبة = مغلق |
| «لا يوجد متصفح 13.52» | يوجد WebKit userland حتى 13.52 (WebKitty/WebKit) — لكن يصل إلى النواة فقط عبر Poopsploit |

---

## 4. المقارنة مع المعرفة المنشورة (المشهد العام، تحديث 2026-09-14)
| المرجع المنشور | موقفنا (13.52) |
|----------------|----------------|
| **Poopsploit 1.8/1.9 (GFM)** يعمل عبر BD-J على 13.52 | ✅ متطابق (خارج المتصفح؛ يتطلب burner Blu-ray) |
| **WebKit userland 13.52 (WebKitty، CSSFontFace)** | ✅ يعمل حتى 13.52 (مع فرضية UFM42)؛ يغلق سطح kernel|
| **GoldHEN 2.3** (منشور) يدعم حتى 13.00 | لا يُثبَّت على 13.52 بدون kernel exploit |
| **UFM42 workaround (نشرت 2026-09-14)** يجلب JS عبر CSSFontFace | يصل إلى WebKit userland لكنه **يُرسل خبرًا إلى Sony**؛ غير منشور المنهج |
| **AES/SLB2 للـ kernel (لا يوجد kernel dump لـ 13.52 عام)** | لم نجد kernel dump محليًا — يعتمد على Poopsploit |

---

## 5. «ما أغفلناه» — ملخّص نهائي للمعرفة
1. **لم نبرمج بعد**: مسبار sc#434/435 (الوحيدان المتبقيان) — يحتاج تمهيدًا نظيفًا.
2. **لم نجرِّ** `?probe=verbatim` (Sonic vs reference gate) حتى الآن؛ إنه الدقة بين «انحراف منفذنا» و«تحصين 13.52».
3. **لم نقدّم** مسبار `?rcl=1` (iov-reclaim 32×) — الخطوة المفقودة بين Poops 1.8 و13.52 في المتصفح.
4. **مسار BD-J**: لا يوجد حارق Blu-ray → لا يمكن تشغيل Poopsploit على الجهاز رغم وجوده (خارج نطاق الاختبار بالمتصفح).
5. **المعرفة الأفدح**: الـ WebKit userland على 13.52 يعمل، لكن **kernel exploit الوحيد الحي = Poopsploit عبر BD-J**؛ وأي مسار kernel آخر من المتصفح = مغلق.

---

## 6. الحكم النهائي
- **من اللعبة/المتصفح (uid=1)**: لا يوجد سطح kernel قابل للوصول — شامل ومغلق.
- **من BD-J (uid=0)**: Poopsploit 13.52 يصل إلى kernel — يتطلب burner Blu-ray (غير متوفر).
- **الخطوات المتبقية الوحيدة**: تمهيد نظيف → سبر 434/435 (جاهز في `tools/stage_434_435_probe.sh`) ثم `?probe=verbatim` + `?rcl=1`.
- **مراقبة المشهد**: `tools/scene_monitor.sh --loop` يعمل، ينتظر علامة `"13.52"` في WebKitty `ps4_offsets.js`.

*نهاية التقرير الشامل — أُعدل أي قسم جديد عند توفر نتيجة جديدة من التمهيد النظيف.*
